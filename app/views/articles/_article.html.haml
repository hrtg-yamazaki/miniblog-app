- @articles.each do |article|
  .article
    .article__box
      .article__box__btn
        
        = link_to "詳細", article_path(article.id), class: "article__box__btn__show"
      .article__box__title
        = article.title
        = link_to "by #{article.user.nickname}", "", class: "article__box__title__user"
      %p.article__box__text
        = article.text
      .article__box__image